/target
backend/target/

# Environment variables (contains sensitive data)
.env
.env.local
.env.*.local
.env.blockchain
.env.production
.env.*
!.env.example
!.env.*.example
blockchain/.env
backend/.env
backend/**/.env
config/**/*.env

# CI: per-service test DATABASE_URLs written by scripts/ci/setup-test-databases.sh
.ci-test-db-env

# Node modules
node_modules/
blockchain/node_modules/
frontend/node_modules/

# Build outputs
dist/
build/
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Terraform (provider binaries, local/crash state — never commit these)
# Note: .terraform.lock.hcl IS committed intentionally (dependency lock).
**/.terraform/*
*.tfstate
*.tfstate.*
errored.tfstate
crash.log
crash.*.log
