apiVersion: v1
kind: Namespace
metadata:
  name: verdyx
  labels:
    app.kubernetes.io/name: verdyx
    app.kubernetes.io/component: threat-intelligence
    app.kubernetes.io/part-of: verdyx-platform
    environment: production
    project: verdyx
  annotations:
    description: "Decentralized threat intelligence marketplace namespace"
    contact: "security-team@verdyx.com"
    version: "1.0.0"
---
# Resource Quota for the namespace
apiVersion: v1
kind: ResourceQuota
metadata:
  name: verdyx-quota
  namespace: verdyx
spec:
  hard:
    requests.cpu: "10"
    requests.memory: 20Gi
    limits.cpu: "20"
    limits.memory: 40Gi
    persistentvolumeclaims: "10"
    services: "20"
    secrets: "20"
    configmaps: "20"
    pods: "50"
---
# Network Policy for security isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: verdyx-network-policy
  namespace: verdyx
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: verdyx
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    - namespaceSelector:
        matchLabels:
          name: monitoring
  egress:
  - {} # Allow all egress for blockchain connectivity and external APIs
---
# Service Account for Verdyx applications
apiVersion: v1
kind: ServiceAccount
metadata:
  name: verdyx-sa
  namespace: verdyx
  labels:
    app.kubernetes.io/name: verdyx
    app.kubernetes.io/component: service-account
automountServiceAccountToken: true