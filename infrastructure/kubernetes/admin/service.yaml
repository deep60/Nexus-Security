apiVersion: v1
kind: Service
metadata:
  name: verdyx-admin-service
  namespace: verdyx
  labels:
    app: verdyx-admin
    component: admin
    service: admin
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8888
    targetPort: http
    protocol: TCP
  selector:
    app: verdyx-admin
  sessionAffinity: None

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: verdyx-admin-sa
  namespace: verdyx
  labels:
    app: verdyx-admin
    component: admin

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: verdyx-admin-role
  namespace: verdyx
rules:
- apiGroups: [""]
  resources: ["configmaps", "secrets"]
  verbs: ["get", "list", "watch"]
- apiGroups: [""]
  resources: ["pods", "services"]
  verbs: ["get", "list"]
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: verdyx-admin-rolebinding
  namespace: verdyx
subjects:
- kind: ServiceAccount
  name: verdyx-admin-sa
  namespace: verdyx
roleRef:
  kind: Role
  name: verdyx-admin-role
  apiGroup: rbac.authorization.k8s.io
